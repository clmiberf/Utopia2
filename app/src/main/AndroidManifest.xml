<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.socer.utopia2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mvp.ui.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.ui.activities.LoginActivity" />
        <activity android:name=".mvp.ui.activities.RegisterActivity" />
        <activity android:name=".mvp.ui.activities.HomeActivity" />
        <activity android:name=".mvp.ui.activities.TimeLimitedTaskActivity" />
        <activity android:name=".mvp.ui.activities.AddTaskActivity" />
        <activity android:name=".mvp.ui.activities.DiscoverContentActivity" />
        <activity android:name=".mvp.ui.activities.MyUserInfoActivity"></activity>
        <activity android:name=".mvp.ui.activities.DiscoverContentActivity"></activity>
        <activity android:name=".mvp.ui.activities.AddTaskActivity" />
        <activity android:name=".mvp.ui.activities.TopicHomepageActivity"></activity>
    </application>

</manifest>